@page
@model MajorProjectPages.Pages.Aisles.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Aisle[0].AisleName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Aisle) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AisleName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AisleID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AisleID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AisleID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
